namespace Utility.Enum
{
    public class Enums
    {
        public enum RoleType
        {
            TANK = 1,
            DPS = 2,
            HEAL = 3
        }

        public enum TransactionType
        {
            Creditor = 1,
            Debtor = 0,
        }
    }
}
